script
  set -a
  . /etc/proxy/proxy.conf
  start on runlevel [2345]
  stop on shutdown
  setuid proxy
  setgid proxy
  exec /usr/bin/proxy socks --daemon -t tcp -p ":$SPORT" -a "$SUSER:$SPASS"
end script